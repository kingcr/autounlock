Add the following files here:

1. Static dropbearmulti
2. Static openssl

3. Encrypted filesystem keyfiles, named .fsk-<filesystem>
4. SSH client keys, named .ssh-key-<slot#>

Remember to edit /etc/initramfs-tools/hooks/autounlock and add files to the copy list
